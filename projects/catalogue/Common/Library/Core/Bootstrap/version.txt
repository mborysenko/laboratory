confVersion = 0.0.0.0	//configuration version. Change it when the main configuration file configuration.xml changes. It's the version of the application.
initVersion = 0.0.0.0	//version of the "SDL.Client.Init.js" package, which is loaded before the main configuration is loaded. Change it when one or more js files from the initial package change. It's the version of the UI library.