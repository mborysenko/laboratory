applicationVersion = 0.0.0.0	//application configuration version. Change it when the main configuration file configuration.xml changes. It's the version of the application. ATTENTION: for applications that were originally deployed with the UI library version 1.0-1.4 use the setting 'confVersion' instead.
libraryVersion = 0.0.0.0		//version of the "SDL.Client.Init.js" package, which is loaded before the main configuration is loaded. Change it when one or more js files from the initial package change. It's the version of the UI library. ATTENTION: for applications that were originally deployed with the UI library version 1.0-1.4 use the setting 'initVersion' instead.